const Users = () => {
    // https://dummyjson.com/users
    return (
        <>
            <h1>User Listing</h1>
            {/* {users?.map((user) => (
                <p key={user.id}>
                    {user.firstName} {user.lastName}
                </p>
            ))} */}
        </>
    );
};

export default Users;
